import { NotificationId } from "../domain/notification-id"

export const NotificationIds = {
  signLayerMode: NotificationId.of("sign_layer_mode"),
  numLayerMode: NotificationId.of("num_layer_mode"),
}
